// This ensures the cart page is rendered dynamically
export const dynamic = 'force-dynamic'

export default function CartLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}

